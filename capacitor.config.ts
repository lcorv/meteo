import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meteo.corv.app',
  appName: 'meteo',
  webDir: 'dist/meteo',
  bundledWebRuntime: false
};

export default config;
